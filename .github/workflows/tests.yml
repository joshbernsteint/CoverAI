name: Tests CI

on: [push, pull_request]

jobs:
  create-envfile:
    runs-on: ubuntu-latest
    steps:
    - run: ls -a
    - name: Make envfile
      uses: SpicyPizza/create-envfile@v1.3.1
      with:
        envkey_CLERK_SECRET_KEY: ${{ secrets.CLERK_KEY }}
        envkey_MONGODB_URI: ${{ secrets.MONGODB }}
        envkey_OPENAI_API_KEY: ${{ secrets.OPENAI }}
        envkey_TEST_TOKEN: ${{ secrets.TESTER }}
        envkey_WEBHOOK_SECRET: ${{ secrets.WEBHOOK }}
        directory: server
        file_name: .env
        fail_on_empty: true
        sort_keys: false
  


  test:
    needs: create-envfile
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Test using Node.js
        uses: actions/setup-node@v1
        with:
          node-version: '18'
      - run: cd server/ && ls && npm install
      - run: cd server/ && npm run test

      - name: Tests ✅
        if: ${{ success() }}
        run: |
          curl --request POST \
          --url https://api.github.com/repos/${{ github.repository }}/statuses/${{ github.sha }} \
          --header 'authorization: Bearer ${{ secrets.GITHUB_TOKEN }}' \
          --header 'content-type: application/json' \
          --data '{
            "context": "tests",
            "state": "success",
            "description": "Tests passed",
            "target_url": "https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          }'

      - name: Tests 🚨
        if: ${{ failure() }}
        run: |
          curl --request POST \
          --url https://api.github.com/repos/${{ github.repository }}/statuses/${{ github.sha }} \
          --header 'authorization: Bearer ${{ secrets.GITHUB_TOKEN }}' \
          --header 'content-type: application/json' \
          --data '{
            "context": "tests",
            "state": "failure",
            "description": "Tests failed",
            "target_url": "https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          }'